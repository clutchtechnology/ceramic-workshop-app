# 页面目录整理总结

## ✅ 已完成的目录整理

### 目录结构

```
lib/presentation/pages/
├── roller_kiln/                    # 辊道窑模块
│   ├── roller_kiln_page.dart       # 辊道窑页面（旧版）
│   └── roller_kiln_page_v2.dart    # 辊道窑页面（新版）
│
├── rotary_kiln/                    # 回转窑模块
│   ├── rotary_kiln_page_fixed.dart # 回转窑页面（固定布局）
│   └── rotary_kiln_page_v2.dart    # 回转窑页面（滚动布局）
│
├── scr_equipment/                  # SCR设备模块
│   └── scr_equipment_page.dart
│
├── workshop_monitor/               # 综合监控模块
│   └── workshop_monitor_page.dart
│
├── grinding_workshop/              # 磨料车间模块
│   ├── grinding_workshop_page.dart
│   └── material_feed_page.dart     # 下料监控页面
│
├── settings/                       # 系统设置模块
│   ├── settings_page.dart          # 设置页面（旧版）
│   ├── settings_page_v2.dart       # 设置页面（新版）
│   └── grinding_workshop_settings_page.dart
│
├── common/                         # 通用页面
│   ├── digital_twin_page.dart      # 数字孪生页面
│   └── hmi_demo_page.dart          # HMI演示页面
│
├── navigation/                     # 导航相关页面
│   └── home_page.dart              # 旧版主页
│
└── main_navigation_page.dart       # 主导航页面（应用入口）
```

## 📋 为什么某些页面保留在根目录？

### 1. `main_navigation_page.dart` - 保留在根目录

**原因：**
- 这是应用的**入口页面**，被 `main.dart` 直接引用
- 作为整个应用的**导航容器**，管理所有子页面
- 类似于 `main.dart` 在项目根目录的作用

**类比：**
```
项目根目录/
├── main.dart              # 应用入口
└── lib/
    └── presentation/
        └── pages/
            └── main_navigation_page.dart  # 页面入口
```

**导入路径：**
```dart
// main.dart
import 'presentation/pages/main_navigation_page.dart';
```

如果放在子目录，导入路径会变长：
```dart
// 不推荐：路径变长
import 'presentation/pages/navigation/main_navigation_page.dart';
```

### 2. `common/` 目录中的页面 - 跨模块通用功能

#### `digital_twin_page.dart`
- **功能**：数字孪生可视化，展示整个车间的3D模型
- **使用场景**：可能被多个模块引用（辊道窑、回转窑、SCR设备等）
- **归类**：通用功能，不属于具体业务模块
- **位置**：`common/digital_twin_page.dart`

#### `hmi_demo_page.dart`
- **功能**：HMI组件演示和测试页面
- **使用场景**：开发/测试阶段使用，展示UI组件库
- **归类**：开发工具，不属于业务功能
- **位置**：`common/hmi_demo_page.dart`

### 3. `navigation/` 目录中的页面

#### `home_page.dart`
- **状态**：旧版主页，可能已废弃
- **位置**：`navigation/home_page.dart`
- **说明**：如果不再使用，可以考虑删除

## 📝 命名规范

### 目录命名
- ✅ 小写字母 + 下划线：`rotary_kiln`, `workshop_monitor`
- ❌ 避免：`RotaryKiln`, `rotary-kiln`, `rotaryKiln`

### 文件命名
- ✅ 小写字母 + 下划线：`rotary_kiln_page_fixed.dart`
- ✅ 版本后缀：`_v2`, `_fixed` 表示不同版本
- ❌ 避免：`RotaryKilnPage.dart`, `rotary-kiln-page.dart`

### 类命名
- ✅ 大驼峰：`RotaryKilnPageFixed`
- ✅ 与文件名对应：文件名 `rotary_kiln_page_fixed.dart` → 类名 `RotaryKilnPageFixed`

## 🔄 导入路径更新

所有导入路径已更新为相对路径：

```dart
// ✅ 正确：使用相对路径
import 'workshop_monitor/workshop_monitor_page.dart';
import 'grinding_workshop/grinding_workshop_page.dart';
import 'common/digital_twin_page.dart';

// ❌ 错误：旧路径（已更新）
import 'workshop_monitor_page.dart';
```

## 📊 模块分类说明

| 模块 | 目录 | 说明 |
|------|------|------|
| **设备监控** | `rotary_kiln/`, `roller_kiln/`, `scr_equipment/` | 具体设备监控页面 |
| **综合功能** | `workshop_monitor/` | 整合多个设备的综合监控 |
| **业务模块** | `grinding_workshop/` | 磨料车间相关页面 |
| **系统功能** | `settings/` | 系统配置和设置 |
| **通用功能** | `common/` | 跨模块使用的通用页面 |
| **导航** | `navigation/` | 导航相关页面 |
| **入口** | 根目录 | 应用入口页面 |

## ✅ 整理完成检查清单

- [x] 创建新目录结构
- [x] 移动文件到对应目录
- [x] 更新所有导入引用
- [x] 检查lint错误
- [x] 验证文件路径正确性
- [x] 创建文档说明

## 🎯 后续建议

1. **删除废弃页面**：如果 `home_page.dart` 不再使用，可以考虑删除
2. **统一版本管理**：考虑统一使用 `_v2` 版本，逐步废弃旧版本
3. **添加导出文件**：可以为每个模块创建 `export.dart` 统一导出，简化导入

